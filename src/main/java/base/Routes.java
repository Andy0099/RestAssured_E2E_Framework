package base;

public class Routes {
    // We store the paths here. If the URL changes, we only fix it here!
    public static String post_endpoint = "/posts";
    public static String get_endpoint = "/posts/{id}"; 
}
